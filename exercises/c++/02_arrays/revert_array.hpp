//Read the given number of elements of type T from stdin
template <typename T>
T *read_array(int);
//Print to stdout array of type T in reverse order
template <typename T>
void print_reverted(T *, int);
